#include<stdio.h>

void main()
{
    int a,b,sum;
    printf("Enter two numbers: ");
    scanf("%d%d",&a,&b);
    sum = a + b;
    printf("The sum is %d\n",sum);
    printf("%d + %d = %d",a,b,sum);

    // x = a*b + a*b*c;
    // The value of X is 8      (if a=1, b=2, c=3)
    //1 x 2 + 1 x 2 x 3 = 8


 //10 + 20 = 30
    //printf("Hello!\nMy name is Rahul\tPathak");
    //return 0;
}
